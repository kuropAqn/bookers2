<h2>User info</h2>

<table class='table'>
  <tr>
    <%= image_tag user.get_profile_image %>  <!-- 固定値になっているので変数を使いましょう -->
  </tr>
  <tr>
    <th>name</th>
    <th><%= user.name %></th>  <!-- 固定値になっているので変数を使いましょう -->
  </tr>
  <tr>
    <th>introduction</th>
    <th>   <!-- 固定値になっているので変数を使いましょう -->
    <% if current_user.introduction.present? %>
    <%= current_user.introduction %>
  <% else %>
    まだ自己紹介文が設定されていません。
  <% end %></th>  
  </th>
  </tr>
</table>　
<div class='row'>   <!-- 編集リンクが未実装 -->
   <%= link_to edit_user_path(current_user.id), class: 'btn btn-outline-secondary btn-block' do %>
  <i class="fa-solid fa-user-gear"></i> Edit <% end %>


   
</div>
<h2 class="mt-3">New book</h2>  <!-- form_withを用いて新規投稿フォームを作成しましょう。 -->
  <%= form_with model: book, url: '/books', method: :post do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title, class: "form-control" %>

    <h4>Body</h4>
    <%= f.text_area :body, class: "form-control" %>

    <%= f.submit 'Create Book', class: "btn btn-success" %>
  <% end %>
</div> 
<div class='container px-5 px-sm-0'>
	<div class='row'>
	  <div class='col-md-3'>
	    <h2>User info</h2>
	    <table class='table'>
	      <tr>
	        <%= image_tag @user.profile_image_url %>  <!-- 固定値になっているので変数を使いましょう -->
	      </tr>
	      <tr>
	        <th>name</th>
	        <th><%= @user.name %></th>  <!-- 固定値になっているので変数を使いましょう -->
	      </tr>
	      <tr>
	        <th>introduction</th>
	        <th><%= @user.introduction %></th>  <!-- 固定値になっているので変数を使いましょう -->
	      </tr>
	    </table>
	    <div class='row'>
	      <%= link_to 'Edit', edit_user_path(current_user.id) %>
	    </div>
	    <h2 class="mt-3">New book</h2>
	    <%= form_with @book :name : each do |new| %>
	  </div>
	  <div class='col-md-8 offset-md-1'>
	    <h2>Books</h2>
    			<%= form_with @books each do |book| %>
	    <table class='table table-hover table-inverse'>
	      <thead>
	        <tr>
	          <th></th>
	          <th>Title</th>
	          <th>Opinion</th>
	          <th colspan="3"></th>
	        </tr>
	      </thead>
	      <tbody></tbody>
	    </table>
	  </div>
	</div>
</div>
